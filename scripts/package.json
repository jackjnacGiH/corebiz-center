{
  "name": "corebiz-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Utility scripts for CoreBiz Center: Obsidian Vault sync, data import, etc.",
  "scripts": {
    "sync": "tsx sync-obsidian.ts",
    "sync:force": "tsx sync-obsidian.ts --force",
    "sync:cleanup": "tsx sync-obsidian.ts --cleanup",
    "sync:dry": "tsx sync-obsidian.ts --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "gray-matter": "^4.0.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
